export type GameInfo = {

    gameAbandoned: boolean,
    gameCreator: string,
    gamePaused: boolean,
    hasEnded: boolean,
    hasStarted: boolean,
    itemCount: number,
    minStake: number,
    playersCount: number,
    totalStaked: number,
    winner: string
}